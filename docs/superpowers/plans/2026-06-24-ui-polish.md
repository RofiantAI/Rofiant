# UI Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Polish the Rofiant dark-theme marketing site with 8 targeted UI improvements — fix broken colors, add hover states, scroll reveals, subtle animations, and better gradients.

**Architecture:** CSS-first approach. Most changes are Tailwind class updates in existing components. One new client component (ScrollReveal). Animations via CSS keyframes in globals.css. No new dependencies.

**Tech Stack:** Next.js 16, React 19, Tailwind CSS v4, CSS custom properties

---

### Task 1: Add CSS Keyframes & Smooth Scroll to globals.css

**Files:**
- Modify: `src/app/globals.css`

- [ ] **Step 1: Add smooth scroll to html**

Add at the top of the `body` rule (line 110):

```css
html {
  scroll-behavior: smooth;
}
```

- [ ] **Step 2: Add pulse-glow keyframe**

Add after the existing `.glow-blue` utility (after line 137):

```css
@keyframes pulse-glow {
  0%, 100% { opacity: 0.12; }
  50% { opacity: 0.25; }
}

@keyframes marquee {
  0% { transform: translateX(0); }
  100% { transform: translateX(-50%); }
}
```

- [ ] **Step 3: Verify dev server starts**

Run: `bun run dev`
Expected: Server starts on localhost:3000 without errors

---

### Task 2: Create ScrollReveal Component

**Files:**
- Create: `src/components/ui/scroll-reveal.tsx`

- [ ] **Step 1: Write the component**

```tsx
"use client";

import { type ReactNode, useRef, useEffect, useState } from "react";

interface ScrollRevealProps {
  children: ReactNode;
  delay?: number;
  className?: string;
}

export function ScrollReveal({
  children,
  delay = 0,
  className = "",
}: ScrollRevealProps) {
  const ref = useRef<HTMLDivElement>(null);
  const [isVisible, setIsVisible] = useState(false);

  useEffect(() => {
    const el = ref.current;
    if (!el) return;

    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          setIsVisible(true);
          observer.unobserve(el);
        }
      },
      { threshold: 0.1 }
    );

    observer.observe(el);
    return () => observer.disconnect();
  }, []);

  return (
    <div
      ref={ref}
      className={className}
      style={{
        opacity: isVisible ? 1 : 0,
        transform: isVisible ? "translateY(0)" : "translateY(20px)",
        transition: `opacity 600ms cubic-bezier(0.4, 0, 0.2, 1) ${delay}ms, transform 600ms cubic-bezier(0.4, 0, 0.2, 1) ${delay}ms`,
      }}
    >
      {children}
    </div>
  );
}
```

- [ ] **Step 2: Verify no TypeScript errors**

Run: `npx tsc --noEmit`
Expected: No errors

---

### Task 3: Fix Scale Section Background

**Files:**
- Modify: `src/components/sections/scale-section.tsx`

- [ ] **Step 1: Replace background class and add gradient overlay**

Change line 6:
```
- className="py-24 bg-[#c7d2fe]"
+ className="py-24 bg-background relative overflow-hidden"
```

Add as first child inside the section div (after line 7):
```tsx
<div className="absolute inset-0 bg-[radial-gradient(ellipse_at_center,rgba(59,130,246,0.08)_0%,transparent_70%)]" />
```

- [ ] **Step 2: Fix all text color classes**

Replace throughout the file:
- `text-gray-900` → `text-foreground`
- `text-gray-700` → `text-foreground-secondary`
- `text-gray-600` → `text-foreground-muted`
- `text-gray-400` → `text-foreground-muted`

- [ ] **Step 3: Fix button variant**

Change line 27:
```
- <Button variant="primary" size="md" className="bg-gray-900 text-white hover:bg-gray-800">
+ <Button variant="primary" size="md">
```

- [ ] **Step 4: Verify in browser**

Run: `bun run dev`, navigate to page, scroll to Scale section
Expected: Dark background with faint blue radial gradient, white/gray text, consistent with rest of site

---

### Task 4: Header Refinement

**Files:**
- Modify: `src/components/sections/header-section.tsx`

- [ ] **Step 1: Update header classes**

Change line 9:
```
- <header className="sticky top-0 z-50 w-full border-b border-border bg-foreground">
+ <header className="sticky top-0 z-50 w-full border-b border-border/50 bg-foreground/80 backdrop-blur-xl">
```

- [ ] **Step 2: Verify in browser**

Scroll down the page. Header should show content blurred behind it. Border should be subtle.

---

### Task 5: Hero SVG Animation

**Files:**
- Modify: `src/components/sections/hero-section.tsx`

- [ ] **Step 1: Add animation to yellow glow circles**

Lines 133-134, add `style` prop:
```tsx
<circle cx="453" cy="421" r="28" fill="#eab308" opacity="0.12" style={{ animation: "pulse-glow 4s ease-in-out infinite" }} />
<circle cx="453" cy="421" r="18" fill="#eab308" opacity="0.08" />
```

- [ ] **Step 2: Add animation to green glow circles**

Lines 139-140, add `style` prop:
```tsx
<circle cx="843" cy="413" r="28" fill="#84cc16" opacity="0.12" style={{ animation: "pulse-glow 4s ease-in-out infinite 2s" }} />
<circle cx="843" cy="413" r="18" fill="#84cc16" opacity="0.08" />
```

- [ ] **Step 3: Verify in browser**

Hero section should show subtle pulsing glow on both highlighted nodes, offset by 2s.

---

### Task 6: Card Hover States

**Files:**
- Modify: `src/components/ui/card.tsx`

- [ ] **Step 1: Update bordered variant**

Change line 9:
```
- bordered: "bg-card border border-border",
+ bordered: "bg-card border border-border transition-all duration-300 hover:border-border-light hover:shadow-lg hover:-translate-y-0.5",
```

- [ ] **Step 2: Update elevated variant**

Change line 10:
```
- elevated: "bg-card border border-border shadow-lg",
+ elevated: "bg-card border border-border shadow-lg transition-all duration-300 hover:border-border-light hover:shadow-xl hover:-translate-y-0.5",
```

- [ ] **Step 3: Verify in browser**

Hover over cards in Unify and Deploy sections. Should see subtle lift + border brightening.

---

### Task 7: Button Hover Polish

**Files:**
- Modify: `src/components/ui/button.tsx`

- [ ] **Step 1: Update primary variant**

Change line 12:
```
- primary: "bg-button-primary text-button-primary-foreground hover:bg-foreground/90",
+ primary: "bg-button-primary text-button-primary-foreground hover:bg-foreground/90 hover:shadow-sm",
```

- [ ] **Step 2: Update outline variant**

Change line 16:
```
- outline: "bg-button-outline text-button-outline-foreground border border-border hover:bg-background-tertiary",
+ outline: "bg-button-outline text-button-outline-foreground border border-border hover:bg-background-tertiary hover:border-border-light",
```

- [ ] **Step 3: Update ghost variant**

Change line 18:
```
- ghost: "bg-transparent text-foreground hover:bg-background-tertiary",
+ ghost: "bg-transparent text-foreground hover:bg-background-tertiary hover:shadow-sm",
```

---

### Task 8: Footer CTA Gradient

**Files:**
- Modify: `src/components/sections/footer-cta-section.tsx`

- [ ] **Step 1: Replace gradient divs**

Replace lines 7-20 (both gradient divs) with single div:

```tsx
<div
  className="absolute top-1/2 left-1/2 h-[600px] w-[600px] -translate-x-1/2 -translate-y-1/2 rounded-full"
  style={{
    background:
      "radial-gradient(circle, rgba(59,130,246,0.12) 0%, rgba(234,179,8,0.06) 40%, transparent 70%)",
  }}
/>
```

- [ ] **Step 2: Verify in browser**

Footer CTA should have subtle blue-to-yellow dark gradient orb instead of bright white.

---

### Task 9: Logo Cloud Marquee

**Files:**
- Modify: `src/components/sections/logo-cloud-section.tsx`

- [ ] **Step 1: Rewrite as CSS marquee**

Replace entire file content:

```tsx
export function LogoCloudSection() {
  const logos = [
    "Fireworks AI",
    "Cal.com",
    "Mintlify",
    "Symbolica",
    "BlindPay",
    "Magic Patterns",
    "Plain",
  ];

  const doubled = [...logos, ...logos];

  return (
    <section className="py-12 overflow-hidden">
      <div className="flex animate-[marquee_20s_linear_infinite] w-max gap-x-8">
        {doubled.map((logo, i) => (
          <div
            key={`${logo}-${i}`}
            className="text-sm font-medium text-foreground-muted whitespace-nowrap"
          >
            {logo}
          </div>
        ))}
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Verify in browser**

Logos should scroll continuously left. Seamless loop (no gap/jump).

---

### Task 10: Stats Section Gradient Separator

**Files:**
- Modify: `src/components/sections/stats-section.tsx`

- [ ] **Step 1: Replace border with gradient**

Change line 9:
```
- <section className="py-12 border-y border-border">
+ <section className="py-12 relative">
```

Add inside the section, before the content div:
```tsx
<div className="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-border to-transparent" />
<div className="absolute bottom-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-border to-transparent" />
```

- [ ] **Step 2: Verify in browser**

Stats section should have gradient fade-in/fade-out horizontal line instead of hard border.

---

### Task 11: Add ScrollReveal to Page Sections

**Files:**
- Modify: `src/app/page.tsx`

- [ ] **Step 1: Import ScrollReveal**

Add at top of file:
```tsx
import { ScrollReveal } from "@/components/ui/scroll-reveal";
```

- [ ] **Step 2: Wrap sections**

Wrap each section (except Header and Hero) with `<ScrollReveal>`:

```tsx
<main>
  <HeroSection />
  <ScrollReveal>
    <StatsSection />
  </ScrollReveal>
  <ScrollReveal delay={100}>
    <LogoCloudSection />
  </ScrollReveal>
  <ScrollReveal>
    <UnifySection />
  </ScrollReveal>
  <ScrollReveal>
    <DeploySection />
  </ScrollReveal>
  <ScrollReveal>
    <ProtectSection />
  </ScrollReveal>
  <ScrollReveal>
    <ScaleSection />
  </ScrollReveal>
  <ScrollReveal>
    <MonitorSection />
  </ScrollReveal>
  <ScrollReveal>
    <DeveloperSection />
  </ScrollReveal>
  <ScrollReveal>
    <FooterCtaSection />
  </ScrollReveal>
</main>
```

- [ ] **Step 3: Verify full page**

Scroll through entire page. Each section should fade-up into view as it enters viewport.

---

### Task 12: Final Verification

- [ ] **Step 1: Run TypeScript check**

Run: `npx tsc --noEmit`
Expected: No errors

- [ ] **Step 2: Run linter**

Run: `bun run lint`
Expected: No errors

- [ ] **Step 3: Run build**

Run: `bun run build`
Expected: Build succeeds

- [ ] **Step 4: Visual review**

Open localhost:3000, scroll full page. Verify:
- [ ] Header has backdrop blur
- [ ] Hero SVG nodes pulse
- [ ] Stats section gradient borders
- [ ] Logo cloud marquee scrolls
- [ ] Cards lift on hover
- [ ] Scale section is dark (not lavender)
- [ ] Footer CTA has dark gradient
- [ ] All sections fade-in on scroll
